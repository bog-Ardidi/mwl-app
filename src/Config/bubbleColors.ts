import colors from "./colors";

/**
 * The colors for the bubbles on the chart in multi-day selection.
 * Every day add one more of the colors to the graph.
 * E.g. 1 day - only green, 3 days - green, yellow and blue, etc.
 */
const bubbleColors = [
  colors.bubbleGreen,
  colors.yellow,
  colors.lightBlue,
  colors.lightPurple,
  colors.hotPink,
];

export default bubbleColors;
